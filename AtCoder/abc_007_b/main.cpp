#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string A;
    cin >> A;
    if(A.size() == 1 && A[0]=='a'){ cout << "-1" << endl;
    }else                         { cout << 'a' << endl; }
    
    return 0;
}
